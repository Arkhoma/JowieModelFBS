# Runtime + test dependencies, pinned to what this was actually built and
# verified against. Regenerate deliberately, not casually -- a silent
# numpy or pandas major bump can move the ratings.
#
# Install:  pip install -r requirements.txt
#     or:   uv pip install -r requirements.txt

# Web app
fastapi==0.141.1
uvicorn==0.53.0
jinja2==3.1.6
python-multipart==0.0.32

# Model
numpy==2.5.3
pandas==3.0.6
pyarrow==25.0.1

# Analysis tools only (tools/ranking_convergence.py). Not imported by
# anything under src/ -- the shipped model depends on numpy alone.
scipy==1.18.1

# Tests
pytest==9.1.1
